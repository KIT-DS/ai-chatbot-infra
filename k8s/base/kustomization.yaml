apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-chatbot

resources:
- 00-namespace.yaml
# - 01-secrets.yaml
- 02-postgres.yaml
- 03-n8n.yaml
# - 04-gitlab-optional.yaml  # Uncomment if you need GitLab

labels:
- pairs:
    app.kubernetes.io/name: ai-chatbot
    app.kubernetes.io/managed-by: kustomize
