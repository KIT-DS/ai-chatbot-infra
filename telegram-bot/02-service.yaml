---
apiVersion: v1
kind: Service
metadata:
  name: telegram-bot
  namespace: ai-chatbot
  labels:
    app: telegram-bot
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30081
    protocol: TCP
    name: webhook
  selector:
    app: telegram-bot
